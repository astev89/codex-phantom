interface:
  display_name: "Dag Task Execution"
  short_description: "Dag task execution"
  default_prompt: "Use $dag_task_execution to execute tasks in a directed acyclic graph (DAG) efficiently."
policy:
  allow_implicit_invocation: true
